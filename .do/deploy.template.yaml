spec:
  name: neutron-proxy
  services:
    - dockerfile_path: Dockerfile
      git:
        branch: main
        repo_clone_url: https://github.com/digitalocean/neutron-proxy.git
      name: neutron-proxy
